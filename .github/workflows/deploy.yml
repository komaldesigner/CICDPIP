on: 
  push:
    branches:
      - main
name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: Use Node.js 22
      uses: actions/setup-node@v2
      with:
        node-version: '22'
      
    - name: 🔨 Build Project
      run: |
        npm install
        npm run build
    
    - name: 📂 FTP Deploy
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP__SERVER }}
        username: ${{ secrets.FTP__USER }}
        password: ${{ secrets.FTP__PWD }}
        protocol: ${{ secrets.FTP__PROTOCOL }}
        local-dir: ./build/
        local-dir: ./build
        server-dir: /public_html/maalana.com
